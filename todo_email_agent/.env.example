# Azure AD app registration (see README setup section)
AZURE_CLIENT_ID=
AZURE_TENANT_ID=common

# Optional: enables context-aware reply drafting via the Anthropic API.
# Leave blank to fall back to a simple acknowledgement template.
ANTHROPIC_API_KEY=

# Where local state (token cache, task list) is stored.
DATA_DIR=./data
